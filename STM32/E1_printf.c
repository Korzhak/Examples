#include "main.h"
#include "stdio.h"

int main(void) 
{
    while (1)
    {
        printf("Printing string in Target I/O");
        printf("Example of formating int (%d), float (%f), char (%c)", 1, 3.14, 'y');
    }
}